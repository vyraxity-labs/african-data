# Database Connection (Local Docker Postgres)
DATABASE_URL="postgresql://app:app@localhost:5432/african_data"

# Application Ports
PORT_PUBLIC=3000
PORT_ADMIN=3001

# Admin Authentication (Auth.js / NextAuth)
AUTH_SECRET="replace-with-a-secure-random-secret"

# Scheduled Link Monitoring
CRON_SECRET="replace-with-a-secure-cron-secret"
LINK_CHECK_BATCH_SIZE=25
